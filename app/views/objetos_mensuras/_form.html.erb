<%= form_for(objetos_mensura) do |f| %>
  <% if objetos_mensura.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(objetos_mensura.errors.count, "error") %> prohibited this objetos_mensura from being saved:</h2>

      <ul>
      <% objetos_mensura.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
